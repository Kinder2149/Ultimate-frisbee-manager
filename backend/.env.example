# Variables d'environnement pour Ultimate Frisbee Manager Backend

# Base de données
# Dev (SQLite local)
# DATABASE_URL="file:./dev.db"
# Prod (PostgreSQL Render)
# DATABASE_URL="postgresql://username:password@hostname:port/database"
# Supabase (PostgreSQL gratuit)
# Recommandé avec pgbouncer et SSL
# DATABASE_URL="postgresql://postgres:[YOUR_PASSWORD]@db.<ref>.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1&sslmode=require"

# Serveur
PORT=3002
NODE_ENV=development

# CORS (CSV) — sans wildcards. Mettre les URLs exactes. Les previews Vercel sont gérées par logique endsWith côté serveur.
CORS_ORIGINS="http://localhost:4200,https://ultimate-frisbee-manager-kinder.vercel.app"

# Cloudinary
# Recommandé: URL unique (remplace les variables séparées)
# CLOUDINARY_URL="cloudinary://<your_api_key>:<your_api_secret>@dmiqnc2o6"
# Option: variables séparées si pas d'URL
# CLOUDINARY_CLOUD_NAME=dmiqnc2o6
# CLOUDINARY_API_KEY="<your_api_key>"
# CLOUDINARY_API_SECRET="<your_api_secret>"

# Authentification JWT
JWT_SECRET="<long_random_secret>"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_SECRET="<another_long_random_secret>"
JWT_REFRESH_EXPIRES_IN="30d"
