# CHANGELOG

| Date | Mission | Résultat |
|---|---|---|
| 2026-09-10 | Tests serveur, securite et INCIDENT base de production | Suites serveur reecrites (84 tests, 9 suites, base PostgreSQL locale via Docker) ; defauts reels trouves et corriges : image envoyee a Cloudinary avant le controle des droits, import sans espace et capable de modifier un autre espace, modifications repondant 500, fuite de profil a l'inscription. **INCIDENT** : les tests tournaient sur la production (ecrasement de configuration) et l'ont videe ; aucune sauvegarde (offre gratuite). Reconstruit : 12 profils, espaces BASE + Ulti Coach, 71 tags, 74 exercices, 42 termes de lexique. 3 verrous poses, verifies (empreinte de la production identique avant/apres 84 tests). Voir BUGS B6/B7 |
| 2026-09-08 | Intégration Ulti Coach — Mission 1 : Lexique (EN COURS, suite prévue) | Nouveau workspace "Ulti Coach" pour transposer le contenu de formation coachs produit hors dépôt. Table `Lexique` créée (Prisma + migration appliquée en prod via Supabase MCP, `.env` local `DIRECT_URL` périmé contourné), CRUD backend complet sous `/api/lexique`, écran de consultation avec filtre par catégorie (`/lexique`). 41 termes importés depuis la base Notion "Lexique - Vocabulaire commun". Vérifié par le pilote dans le navigateur. Reste : formulaire d'édition, import Exercices/Thèmes-Phases/Séances depuis Notion, écran de déroulement annuel — détail dans PROJET_CONTEXTE.md section 11 |
|---|---|---|
| 2026-09-08 | Audit fonctionnel complet + plan de correction en 9 missions | Audit par navigation reelle (bureau + mobile, roles gestionnaire et lecteur) puis 9 missions correctives verifiees une a une dans le navigateur. **Bloquants** : envoi d'images mobile vers une route inexistante (corrige, l'image accompagne la creation) ; connexion figee sans message quand le chargement du profil echoue (toutes les pannes sont desormais remontees). **Credibilite** : langage technique retire de l'interface, menu « Logs » mort supprime, version/environnement lus depuis /api/health au lieu d'etre ecrits en dur. **Coherence** : 11 routes mobiles de creation protegees par WriteGuard + onglet « Creer » masque aux lecteurs ; vocabulaire unifie en francais (57 libelles, roles via getRoleLabel). **Proprete** : 19 composants morts supprimes dont 6 ecrans inaccessibles (hash de build inchange) ; favicon cree, titre par page, page « introuvable », chevron mobile repare. 2 constats d'audit invalides ecartes apres verification (champ « Points importants » bien persiste ; « requetes en double » = reconnexions pendant une panne). 96 fichiers, +388/-6797. Reste : cle Cloudinary (pilote), delai AuthGuard 5 s, CSS orphelin, terrain mobile mis de cote |
| 2026-09-07 | Rationalisation des services frontend | 6 services morts supprimés (34→28) : aucun usage constaté. Constat posé : les 28 restants sont tous utilisés — pas de fusion artificielle pour atteindre la cible de 20, qui nuirait à la lisibilité. Point "rationalisation" du backlog clos. Log frontend bruyant « Token alg différent de RS256 » retiré (ES256 est désormais le format normal) |
| 2026-09-07 | Service de notification unique | `NotificationManagerService` fusionné dans `NotificationService` (28→27 services) : API riche et API simple conservées toutes les deux. Dernier doublon du backlog résolu. **Reste à valider manuellement** : messages de succès et d'erreur, bouton copier |
| 2026-09-07 | Résidus morts + fichiers de référence des variables | Local resynchronisé sur le dépôt distant (18 commits de retard). Supprimés après vérification qu'aucune référence ne subsiste : `backend/models/entrainement.simple.js`, `core/models/training.simple.ts`, doublon `admin-shell` de settings, `frontend/.env.example` (8 variables `NG_APP_*` lues nulle part). `backend/.env.example` corrigé : ajout de `SUPABASE_URL` et `SUPABASE_SERVICE_ROLE_KEY` — requises par le code mais absentes du fichier de référence, donc installation cassée en suivant l'exemple — port 3002→3000, section outillage local isolée. Port corrigé aussi dans PROJET_CONTEXTE |
| 2026-09-06 | Sécurisation secrets + migration auth ES256 + remise en service prod | Fuite `.env.CLEAN` (repo public) traitée : rotation mot de passe DB + Cloudinary, migration Supabase JWT Signing Keys (ES256). Backend passé en vérification JWKS asymétrique uniquement (HS256 retiré → fuite JWT neutralisée) ; correctif URL JWKS `/auth/v1/.well-known/jwks.json`. `DATABASE_URL` corrigée (pooler). Déployé en prod via PR #2/#3 (Vercel). Connexion + données validées en prod. Topologie clarifiée : Vercel = seule prod, Render abandonné |
| 2026-09-06 | Exécution des corrections d'audit (nettoyage + cohérence) | Sécurité : `.env.CLEAN` retiré du suivi git (rotation secrets = action pilote). Supprimés : 4 specs cassés, doublon `ExerciceService`, 5 services 0-usage, 2 routes mortes (`debug.js`/`swagger`), module `tags-advanced`, feature `mobile-terrain` (+ onglet menu). Archivés : 23 scripts one-shot (5 gardés). Cohérence : décision auth HS256/RS256 alignée. Services frontend 41→34, features 12→11. Reporté : fusion des 2 services notification |
| 2026-09-06 | Resynchronisation documentaire (reprise projet) | Doc realignée sur le code réel : `/api/sync` confirmé monté et actif, `tags-advanced` confirmé toujours présent ET routé (décision figée 2026-04-10 non exécutée — signalée), bugs B1/B2/B2b confirmés résolus via BUGS.md, backlog section 9 réécrit sur l'état réel, retard CHANGELOG comblé |
| 2026-08-20 | Mise à jour PROJET_CONTEXTE (audit de reprise) | Section 10 ajoutée : B1 constaté résolu, retard CHANGELOG signalé, backlog de reprise établi |
| 2026-08-12 | Nettoyage code mort B1/B2 | Suppression `cache-stats.component.ts` (orphelin), `entity-crud.service.spec.ts` (test orphelin), `mobile-detail` (doublon de `mobile-detail-simple`), `admin/pages/activity` (shell vide sans backend). BUGS.md : B1/B2/B2b passés à « Résolu » |
| 2026-07-20 | Nettoyage documentation et corrections services | Exclusion de l'outillage local, corrections diverses de services |
| 2026-04-14 | Initialisation Graphify (robuste) | `.graphifyignore` créé, hook always-on installé (`.claude/settings.json` + `CLAUDE.md`), `graphify-out/` généré (`GRAPH_REPORT.md`, `graph.json`, `graph.html`), `graphify-out/` ajouté à `.gitignore`, `PROJET_CONTEXTE.md` mis à jour |
| 2026-04-10 | Reprise projet — mise en place méthode | PROJET_CONTEXTE.md créé, STACK_STANDARD.md copié à la racine, documentation archivée dans _archives/ (85 fichiers), CHANGELOG.md et BUGS.md initialisés |
