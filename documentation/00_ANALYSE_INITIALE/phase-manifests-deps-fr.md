Toolchain globale cohérente mais à harmoniser: backend fixe Node 20.x (engines) tandis que le frontend n’explicite pas sa version; recommander Node 20.x repo-wide. Angular ~17.3.x avec TypeScript ~5.2.2 côté frontend, alors que la racine déclare TypeScript ^5.9.2: aligner sur ~5.2.x pour la compatibilité Angular 17 ou retirer TS du root s’il est inutile.
Dépendance locale backend "ultimate-frisbee-manager": "file:.." à supprimer/remplacer (risque CI/CD et lockfiles). Prisma utilise sqlite alors que la dépendance "pg" est présente: clarifier la base cible (sqlite vs Postgres) et nettoyer/documenter en conséquence. Scripts backend complets (prisma migrate/seed/test) et stack sécurité (helmet, rate-limit, jsonwebtoken) présents. Côté frontend, @angular/material, rxjs 7.8, zone.js 0.14 et outillage de test (karma, jasmine, cypress) bien installés.
Recommandations immédiates: déclarer engines côté frontend (>=20 <21), aligner TypeScript, exécuter npm outdated/audit dans backend et frontend, et retirer la dépendance locale. Commandes utiles: `npm --prefix frontend outdated`, `npm --prefix backend outdated`, `npm --prefix frontend audit --production --audit-level=moderate`, `npm --prefix backend audit --production --audit-level=moderate`. 